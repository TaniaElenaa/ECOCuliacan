<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>¡Bienvenido(a) - Eco Culiacán</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Tus estilos CSS aquí, si no están en style.css */
        body {
            font-family: sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header.encabezado {
            background-color: #fff;
            padding: 15px 20px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header.encabezado .logo img {
            width: 100px; /* Ajusta según el tamaño de tu logo */
            height: auto;
        }

        /* Estilos para los botones de la cabecera */
        .header-buttons .btn-iniciar-sesion,
        .header-buttons .btn-registrarme,
        .header-buttons .secondary-btn { /* Asegúrate de que este estilo exista para el botón de cerrar sesión */
            background-color: #28a745;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s ease;
            margin-left: 10px;
            text-decoration: none; /* Si son enlaces con estilos de botón */
        }
        .header-buttons .btn-iniciar-sesion:hover,
        .header-buttons .btn-registrarme:hover,
        .header-buttons .secondary-btn:hover {
            background-color: #218838;
        }
        .header-buttons .secondary-btn { /* Ajuste si tu botón de cerrar sesión tiene un color diferente */
            background-color: #6c757d; /* Gris para "Cerrar Sesión" si prefieres */
        }
        .header-buttons .secondary-btn:hover {
            background-color: #5a6268;
        }


        .main-content {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
            text-align: center;
        }

        h1 {
            color: #28a745;
            margin-bottom: 20px;
        }

        .primary-btn {
            background-color: #28a745;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }
        .primary-btn:hover {
            background-color: #218838;
        }

        .secondary-btn {
            background-color: #6c757d;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }
        .secondary-btn:hover {
            background-color: #5a6268;
        }

        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px;
            margin-top: auto;
        }

        footer a {
            color: #007bff;
            text-decoration: none;
        }

        footer .social-icons {
            display: flex;
            justify-content: center;
            margin-top: 10px;
        }

        footer .social-icons a {
            margin: 0 10px;
        }

        footer .social-icons img {
            width: 30px;
            height: auto;
            transition: transform 0.2s ease;
        }
        footer .social-icons img:hover {
            transform: scale(1.1);
        }

        /* Estilos para mensajes flash (copiados de index.ejs, si no están en style.css) */
        .flash-message {
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 5px;
            font-weight: bold;
            text-align: center;
            width: 80%; /* Ajusta el ancho para centrarlo */
            max-width: 400px;
            margin-left: auto;
            margin-right: auto;
        }
        .flash-message.success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .flash-message.error {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
    </style>
</head>
<body>
    <header class="encabezado">
        <div class="logo">
            <a href="/"><img src="/images/ECO.png" alt="Logo Eco Culiacán"/><span></span></a>
        </div>
        <div>
            <% if (user) { %>
                <span style="margin-right: 15px;">
                    <% if (user && user.gender === 'female') { %> 
                        ¡Bienvenida, <%= user.username %>!
                    <% } else if (user && user.gender === 'male') { %> 
                        ¡Bienvenido, <%= user.username %>!
                    <% } else { %>
                        ¡Hola, <%= user.username %>!
                    <% } %>
                </span>
                <a href="/logout"><button class="secondary-btn">Cerrar Sesión</button></a>
            <% } else { %>
                <a href="/login"><button class="btn-iniciar-sesion">Iniciar Sesión</button></a>
                <a href="/registro"><button class="btn-registrarme">Registrarme</button></a>
            <% } %>
        </div>
    </header>

    <main class="main-content">
        <% if (success_msg && success_msg.length > 0) { %>
            <div class="flash-message success">
                <%= success_msg %>
            </div>
        <% } %>
        <% if (error_msg && error_msg.length > 0) { %>
            <div class="flash-message error">
                <%= error_msg %>
            </div>
        <% } %>
        <% if (error && error.length > 0) { %> 
            <div class="flash-message error">
                <%= error %>
            </div>
        <% } %>

        <h1>
            <% if (user) { %> 
                <% if (user.gender === 'female') { %>
                    ¡Bienvenida a tu cuenta!
                <% } else if (user.gender === 'male') { %>
                    ¡Bienvenido a tu cuenta!
                <% } else { %>
                    ¡Bienvenido(a) a tu cuenta!
                <% } %>
            <% } else { %>
                ¡Bienvenido(a) al servicio de recolección!
            <% } %>
        </h1>
        
        <p>Gracias por ser parte de ECO Culiacán.</p>
        <p>Aquí puedes gestionar tus reportes o explorar otras opciones.</p>

        <% if (user) { %>
            <a href="/seleccion-zona" class="primary-btn" style="margin-top: 20px; display: inline-block;">Hacer un Reporte</a>
        <% } else { %>
            <a href="/login" class="primary-btn" style="margin-top: 20px; display: inline-block;">Ingresar</a>
        <% } %>

        <% if (!user) { %>
            <img src="/images/camion.gif" alt="Recolección de Basura" style="margin-top: 30px; max-width: 100%; height: auto;">
        <% } %>

    </main>

    <footer class="pie">
        <p>Menos basura, más futuro</p>
        <p><a href="https://x.com/ECOCuliacan/" target="_blank">https://x.com/ECOCuliacan</a></p>
<div class="social-icons">
    <a href="https://www.facebook.com/share/16Je91rWsc/" target="_blank"><img src="/images/facebook.png" alt="Facebook"></a>
    <a href="https://x.com/ECOCuliacan" target="_blank"><img src="/images/twitter.png" alt="Twitter"></a>
    <a href="https://www.instagram.com/eco.culiacan/profilecard/" target="_blank"><img src="/images/ig.png" alt="Instagram"></a>
</div>

    <script>
        window.onload = function() {
            const successMessageDiv = document.querySelector('.flash-message.success');
            const errorMessageDiv = document.querySelector('.flash-message.error');
            
            const genericErrorPassportDiv = document.querySelector('.flash-message.error'); 

            if (successMessageDiv && successMessageDiv.textContent.trim().length > 0) {
                alert('Mensaje de Éxito: ' + successMessageDiv.textContent.trim());
            } else if (errorMessageDiv && errorMessageDiv.textContent.trim().length > 0) {
                alert('Mensaje de Error: ' + errorMessageDiv.textContent.trim());
            } else if (genericErrorPassportDiv && genericErrorPassportDiv.textContent.trim().length > 0) {
                alert('Mensaje de Error General: ' + genericErrorPassportDiv.textContent.trim());
            }
        };
    </script>
</body>
</html>