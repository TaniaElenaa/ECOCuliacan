<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Iniciar Sesión - Eco Culiacán</title>
    <link rel="stylesheet" href="/css/style.css" /> 
    <style>
        /* Aquí puedes mantener tus estilos CSS específicos para login.ejs si no los tienes en style.css */
        /* O simplemente asegurarte de que style.css contenga todos los estilos necesarios */
        body {
            font-family: sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header.encabezado {
            background-color: #fff;
            padding: 15px 20px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header.encabezado .logo img {
            width: 100px;
            height: auto;
        }

        .contenedor-principal-centrado {
            flex-grow: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        main.contenedor-login {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 90%;
            max-width: 400px;
            text-align: center;
        }

        main.contenedor-login h1 {
            color: #28a745;
            margin-bottom: 20px;
        }

        main.contenedor-login .formulario label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            text-align: left;
        }

        /* Contenedor para el input y el icono de mostrar/ocultar contraseña */
        .password-container {
            position: relative;
            margin-bottom: 15px; /* Mueve el margen a este contenedor */
        }

        .password-container input[type="password"],
        .password-container input[type="text"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            padding-right: 40px; /* Deja espacio para el icono */
        }

        .toggle-password {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            font-size: 1.2em; /* Ajusta el tamaño si usas texto o emoji */
            color: #666;
            background: none; /* Eliminar estilo de botón si es un botón */
            border: none;
            padding: 0;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .toggle-password img {
            width: 24px; /* Ajusta el tamaño del icono de ojo */
            height: 24px;
        }


        main.contenedor-login .formulario button.btn-iniciar {
            background-color: #28a745;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s ease;
            width: 100%; /* Para que el botón ocupe todo el ancho */
            margin-top: 15px; /* Espacio entre el último campo y el botón */
        }
        main.contenedor-login .formulario button.btn-iniciar:hover {
            background-color: #218838;
        }

        /* Estilos para el botón "Registrarme" en el header */
        .btn-registrarme {
            background-color: #007bff; /* Un azul, puedes ajustar el color */
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s ease;
            text-decoration: none; /* Si es un <a> con estilos de botón */
        }
        .btn-registrarme:hover {
            background-color: #0056b3;
        }


        footer.pie {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px;
            margin-top: auto;
        }

        footer.pie a {
            color: #007bff;
            text-decoration: none;
        }

        footer.pie .redes {
            display: flex;
            justify-content: center;
            margin-top: 10px;
        }

        footer.pie .redes a {
            margin: 0 10px;
        }

        footer.pie .redes img.social-icon {
            width: 30px;
            height: auto;
            transition: transform 0.2s ease;
        }
        footer.pie .redes img.social-icon:hover {
            transform: scale(1.1);
        }

        /* Estilos para mensajes flash */
        .flash-message {
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 5px;
            font-weight: bold;
            text-align: center;
        }
        .flash-message.success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .flash-message.error {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

            .iconousuario-container {
            text-align: center; /* Para centrar la imagen */
            margin-bottom: 20px; /* Espacio entre el icono y el título */
        }

        .iconousuario-img {
            width: 100px; /* Tamaño del icono */
            height: auto;
             /* Para hacerlo circular si la imagen lo permite */
            border: 2px solid #ffffff; /* Un borde sutil */
        }
    </style>
</head>
<body>

<header class="encabezado">
    <div class="logo">
        <a href="/"><img src="/images/ECO.png" alt="Logo Eco Culiacán" class="logo"/><span></span></a>
    </div>
    <a href="/registro"><button class="btn-registrarme">Registrarme</button></a>
</header>

<div class="contenedor-principal-centrado">
    <main class="contenedor-login">
                <div class="iconousuario-container">
            <img src="/images/usuario.png" alt="Icono de Usuario" class="iconousuario-img">
        </div>

        <h1>Iniciar Sesión</h1>

        <% if (success_msg && success_msg.length > 0) { %>
            <div class="flash-message success">
                <%= success_msg %>
            </div>
        <% } %>
        <% if (error_msg && error_msg.length > 0) { %>
            <div class="flash-message error">
                <%= error_msg %>
            </div>
        <% } %>
        <% if (error && error.length > 0) { %> <div class="flash-message error">
                <%= error %>
            </div>
        <% } %>

        <form class="formulario" action="/login" method="POST">
            <label for="usuario">Usuario</label>
<div class="input-container"> 
    <input type="text" id="usuario" name="username" placeholder="Ingresa tu usuario" required/> 
    <span class="input-icon" onclick="alert('Clic en el icono de usuario!')">
        <img src="/images/usuariop.png" alt="Icono de Usuario">
    </span>
</div>
            <label for="contrasena">Contraseña</label>
            <div class="password-container">
                <input type="password" id="contrasena" name="password" placeholder="Contraseña" required/> 
                <span class="toggle-password" onclick="togglePasswordVisibility('contrasena', this)">
                    <img src="/images/ojo.png" alt="Mostrar/Ocultar Contraseña" id="toggleIcon">
                </span>
            </div>
            
            <button type="submit" class="btn-iniciar">Iniciar Sesión</button>
        </form>
        <p style="margin-top: 20px;">¿No tienes una cuenta? <a href="/registro">Regístrate aquí</a></p>
    </main>
</div>

<footer class="pie">
    <p><em>Menos basura, más futuro</em></p>
        <a href="https://x.com/ECOCuliacan" target="_blank">https://x.com/ECOCuliacan</a>
<div class="social-icons">
    <a href="https://www.facebook.com/share/16Je91rWsc/" target="_blank"><img src="/images/facebook.png" alt="Facebook"></a>
    <a href="https://x.com/ECOCuliacan" target="_blank"><img src="/images/twitter.png" alt="Twitter"></a>
    <a href="https://www.instagram.com/eco.culiacan/profilecard/" target="_blank"><img src="/images/ig.png" alt="Instagram"></a>
</div>
</footer>

<script>
    function togglePasswordVisibility(fieldId, iconElement) {
        const passwordField = document.getElementById(fieldId);
        const toggleIcon = iconElement.querySelector('img');

        if (passwordField.type === 'password') {
            passwordField.type = 'text';
            toggleIcon.src = '/images/ojo.png'; // Cambia al icono de ojo abierto
            toggleIcon.alt = 'Ocultar Contraseña';
        } else {
            passwordField.type = 'password';
            toggleIcon.src = '/images/ojoff.png'; // Cambia al icono de ojo cerrado
            toggleIcon.alt = 'Mostrar Contraseña';
        }
    }
</script>

</body>
</html>